"use client"; import {useState} from "react"; import {polls} from "@/data/mock";
export default function PollsPage(){const [voted,setVoted]=useState<Record<number,string>>({});return <div className="stack"><div className="pageTitle"><span className="eyebrow">QUICK POLL</span><h1>짧게 답하고, 구단 운영에 참여하세요.</h1><p>상황별 1~3문항 설문으로 팬의 니즈를 자주, 가볍게 수집합니다.</p></div><div className="grid2">{polls.map(p=><article className="panel" key={p.id}><div className="tag">진행중</div><h2>{p.title}</h2>{!voted[p.id]?<div className="optionList">{p.options.map(o=><button key={o.label} onClick={()=>setVoted({...voted,[p.id]:o.label})}>{o.label}</button>)}</div>:<><div className="success">참여 완료 · +5 YP 적립</div><div className="resultList">{p.options.map(o=><div key={o.label}><div className="resultHead"><span>{o.label}</span><b>{o.value}%</b></div><div className="bar"><i style={{width:`${o.value}%`}}/></div></div>)}</div></>}</article>)}</div></div>}
