import type { Player } from "./types";

// 실제 선수단 명단/사진이 아직 저장소에 없어 임의로 실제 사람 이름을 만들어
// 넣지 않았다. 아래 배열에 실제 데이터(등번호/이름/포지션/사진)만 추가하면
// FAN ZONE의 "선수 응원" 탭에 자동으로 선수 카드가 나타난다.
export const players: Player[] = [];
